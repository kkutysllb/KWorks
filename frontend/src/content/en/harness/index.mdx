---
title: Install KWorks Harness
description: The KWorks Harness is the Python SDK and runtime foundation for building your own Super Agent systems.
---

import { Callout, Cards } from "nextra/components";

# Install KWorks Harness

<Callout type="info" emoji="📦">
  The KWorks Harness Python package will be published as <code>kkworks</code>
  . It is not released yet, so installation is currently{" "}
  <strong>Coming Soon</strong>.
</Callout>

The KWorks Harness is the Python SDK and runtime foundation for building your own Super Agent systems.

If you want to compose agents with skills, memory, tools, sandboxes, and subagents inside your own product or workflow, this is the part of KWorks you will build on.

## Package name

The package name will be:

```bash
pip install kkworks
```

That package is not publicly available yet, but this is the installation path the documentation will use once it is released.

## Current status

The KWorks Harness package is **coming soon**.

At the moment, this section exists to establish the SDK entry point and package identity, while the public distribution flow is being finalized.

## What the Harness will give you

The Harness is designed for developers who want to build their own agent system on top of KWorks's runtime model.

It will provide the foundation for:

- building long-horizon agents,
- composing runtime capabilities such as memory, tools, skills, and subagents,
- running agents with sandboxed execution,
- customizing agent behavior through configuration and code, and
- integrating KWorks into your own application architecture.

## What to do next

Until the package is released, the best way to understand the KWorks Harness is to read the conceptual and implementation docs in this section.

<Cards num={2}>
  <Cards.Card title="Quick Start" href="/en/docs/harness/quick-start" />
  <Cards.Card title="Configuration" href="/en/docs/harness/configuration" />
</Cards>
